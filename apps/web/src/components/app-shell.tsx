"use client";
import Link from "next/link"; import { usePathname, useRouter } from "next/navigation"; import { useAuth } from "@/features/auth/auth-context";
export function AppShell({children}:{children:React.ReactNode}){const {user,logout}=useAuth();const router=useRouter();const path=usePathname();return <div className="min-h-screen"><header className="sticky top-0 z-20 border-b border-slate-800 bg-[#070b16e8] backdrop-blur"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-4"><Link href="/dashboard" className="text-xl font-black"><span className="text-amber-400">Java</span>Quets</Link><nav className="hidden gap-2 md:flex"><Link className={`rounded-lg px-3 py-2 text-sm ${path==="/dashboard"?"bg-slate-800":"text-slate-400"}`} href="/dashboard">Overview</Link><Link className={`rounded-lg px-3 py-2 text-sm ${path.startsWith("/courses")?"bg-slate-800":"text-slate-400"}`} href="/courses">Courses</Link></nav><div className="flex items-center gap-3"><span className="hidden text-sm text-slate-400 sm:block">{user?.displayName??user?.email}</span><button className="btn-secondary !px-3 !py-2 text-sm" onClick={()=>logout().then(()=>router.replace("/login"))}>Log out</button></div></div></header><main className="mx-auto max-w-7xl px-5 py-8">{children}</main></div>}
