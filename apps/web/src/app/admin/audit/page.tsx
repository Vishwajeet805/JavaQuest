"use client";
import {useEffect,useState}from "react";import type{ContentAuditEventDto}from "@javaquets/shared";import {listAudit}from "@/services/admin";import {ErrorState,LoadingState}from "@/components/states";
export default function AuditPage(){const[items,setItems]=useState<ContentAuditEventDto[]|null>(null);const[error,setError]=useState("");useEffect(()=>{listAudit().then(setItems).catch(e=>setError(e instanceof Error?e.message:"Could not load audit trail"))},[]);return <><p className="eyebrow">Accountability</p><h1 className="mt-3 text-4xl font-black">Content audit trail</h1><div className="mt-8">{error?<ErrorState message={error}/>:!items?<LoadingState/>:<div className="card overflow-x-auto"><table className="w-full text-left text-sm"><thead className="text-slate-500"><tr><th className="pb-3">When</th><th>Admin</th><th>Action</th><th>Entity</th><th>Slug</th></tr></thead><tbody>{items.map(i=><tr className="border-t border-slate-800" key={i.id}><td className="py-3 text-slate-400">{new Date(i.createdAt).toLocaleString()}</td><td>{i.adminEmail}</td><td><span className="rounded bg-slate-800 px-2 py-1 text-xs">{i.action}</span></td><td>{i.entityType}</td><td className="text-slate-400">{i.entitySlug??"—"}</td></tr>)}</tbody></table></div>}</div></>}
